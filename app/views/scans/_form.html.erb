<%= form_for(@scan) do |f| %>
  <% if @scan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scan.errors.count, "error") %> prohibited this scan from being saved:</h2>

      <ul>
      <% @scan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :scantype_id %><br />
    <%= f.number_field :scantype_id %>
  </div>
  <div class="field">
    <%= f.label :treatment_id %><br />
    <%= f.number_field :treatment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
