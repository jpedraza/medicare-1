<%= form_for(@scantype) do |f| %>
  <% if @scantype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scantype.errors.count, "error") %> prohibited this scantype from being saved:</h2>

      <ul>
      <% @scantype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
